# Generates packets to parse for 2018 Hack the North

The requirements are at http://pad/test-packet-comms-pad

## Usage
`./bin/generate > ./.packets`

## Configuration
`vi ./config/packet_comms.yml`
